// register — ruta de inregistrare a aplicatiei EcoLocation (in afara tab-urilor).
// Redirecteaza catre RegisterScreen din modulul auth.
import { RegisterScreen } from '../modules/auth';

export default RegisterScreen;
