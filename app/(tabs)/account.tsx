// (tabs)/account — ruta tab-ului de profil al utilizatorului.
// Redirecteaza catre AccountScreen din modulul account pentru vizualizarea si gestionarea contului.
import { AccountScreen } from '../../modules/settings';

export default AccountScreen;
