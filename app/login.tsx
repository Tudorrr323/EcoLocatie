// login — ruta de autentificare a aplicatiei EcoLocation (in afara tab-urilor).
// Redirecteaza catre LoginScreen din modulul auth.
import { LoginScreen } from '../modules/auth';

export default LoginScreen;
